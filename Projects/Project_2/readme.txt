Project 2:

Objective:
To use the if selection statement.

In this project, prompt the user to enter a number up 20, and the the program will display all the prime number up to that number.

There are multiple way to do this, but the solution displayed uses the if and the else if statement to accomplish this.


Ensure to know:

-if and elseif statement.

-Logical operators such as  >, >=, <=, ==, and &&.